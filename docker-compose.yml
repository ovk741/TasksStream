services:
  postgres:
    image: postgres:15
    container_name: taskstream_postgres
    environment:
      POSTGRES_DB: taskstream
      POSTGRES_USER: taskstream
      POSTGRES_PASSWORD: taskstream
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: